name: ziyan_app
description: "Boşa harcanan zamanı takip eden uygulama"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0-162.1.beta

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Veritabanı
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Grafikler
  fl_chart: ^0.69.0

  # Bildirimler
  flutter_local_notifications: ^18.0.1

  # Arka plan servisi
  workmanager: ^0.9.0

  # Durum yönetimi
  provider: ^6.1.2

  # Tarih işlemleri
  intl: ^0.19.0

  # İkonlar
  flutter_vector_icons: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
